
            } // Template: NPCLevel3Part2_cs.txt
            if (NeoTrace.RUNTIME) LogExt("Bury(skey).#CLASSNAME#", e);
            return e; // return Entity e to signal if key is Missing or bad key
        }
    }
}